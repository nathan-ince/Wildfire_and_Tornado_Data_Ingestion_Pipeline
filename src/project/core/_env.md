# **_env.md**

This file reads and processes environment variables.

This file exports a single instance of `Settings`, which is what we can use to access data from environment variables

The purpose of doing this is so that we can process all of our environment variables in one place and then access them from one object.

---
