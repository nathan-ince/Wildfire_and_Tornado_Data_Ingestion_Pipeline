# **pipelines**

This folder contains a folder for each pipeline in this project.

Each pipeline folder exports a function to run the pipeline, which should be called in a script.

Each pipeline has a corresponding config file that will be processed.

---
