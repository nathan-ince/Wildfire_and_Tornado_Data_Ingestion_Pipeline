# **_log.md**

This file exports a function that can be called to configure logging.

The exported function should only be called once at the start of a script.

This configuration is intended for our primary scripts.

We have a separate logging configuration for tests.

---
