# **pandas**

This folder contains some things that are specific to `pandas`.

A lot of it includes small reusable functions to process data in a specific way.

A lot if not all of these functions will be used in the `pipelines` folder.

Having these functions helps reduce boilerplate.

---
